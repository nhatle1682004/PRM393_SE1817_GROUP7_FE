name: Identity Service CI/CD

on:
  push:
    branches:
      - BE
    paths:
      - 'src/Services/IdentityService/**'
      - 'src/Shared/**'
      - 'docker-compose.yml'
      - '.github/workflows/identity-service.yml'

  pull_request:
    branches:
      - BE
    paths:
      - 'src/Services/IdentityService/**'
      - 'src/Shared/**'
      - 'docker-compose.yml'
      - '.github/workflows/identity-service.yml'

  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: identity-service-cicd-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/prm393-group7-identity


jobs:
  ci-cd:
    name: Build and publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/Services/IdentityService/IdentityService.Api/IdentityService.Api.csproj

      - name: Build
        run: >
          dotnet build
          src/Services/IdentityService/IdentityService.Api/IdentityService.Api.csproj
          --configuration Release
          --no-restore

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: src/Services/IdentityService/IdentityService.Api/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
